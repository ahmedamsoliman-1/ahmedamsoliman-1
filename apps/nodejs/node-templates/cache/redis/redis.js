// const { redis } = require('./redisConnector');

// var redisHelpers = {};

// redisHelpers.listRedisKeys = async function () {
//   const all_redis_keys = await redis.keys(`*`);
//   return all_redis_keys;
// };

// redisHelpers.listRedisInfo = async function () {
//   console.log(redis)
//   const redis_info = await redis.info();
//   return redis_info;
// };

// module.exports = redisHelpers;
